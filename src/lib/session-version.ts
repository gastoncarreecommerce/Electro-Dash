// Bump this value (e.g. to a new date) to force-logout every active session:
// existing cookies stop matching and the middleware sends everyone back to /login.
export const SESSION_VERSION = "2026-09-04";
